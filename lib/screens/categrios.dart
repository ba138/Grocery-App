import 'package:flutter/material.dart';

class CategriousScreen extends StatelessWidget {
  const CategriousScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Categrious screen"),
      ),
    );
  }
}
